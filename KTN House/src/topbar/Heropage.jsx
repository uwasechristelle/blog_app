import React from "react";

export const Heropage = ({ title }) => {
  return (
    <div className="img">
      <div className="hero-title">
        {/* <h1>{title}</h1> */}
      </div>
    </div>
  );
};
